services:
  wiki:
    build: .
    ports:
      - "${FOLDER_WIKI_PORT:-4173}:4173"
    volumes:
      - "${FOLDER_WIKI_CONTENT:-./content}:/app/content"
      - "${FOLDER_WIKI_DATA:-./.folder-wiki}:/app/data"
    restart: unless-stopped
