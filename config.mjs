// This is intentionally a simple local password, not a user/authentication system.
// Change it here and restart the server when you want a different admin password.
export const ADMIN_PASSWORD = "gmrules";
